#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main()
{
    string dm = "abbas";
    int ind = 0;
    for (int i = 0; i < dm.length(); i++)
    {

        cout << (int)dm[i];
        ind += (int)dm[i];
    }

    index = HashFunction(ind;)
    return 0;
}